#ifndef __WittedMouse__MouseUtil__
#define __WittedMouse__MouseUtil__

enum MouseAction {
	RUN = 1,
	JUMP,
	SPEEDUP,
	SLOWDOWN
};

enum ItemType {
	NULLTYPE,
	DENGEROUS_CUCRBIT,
	BENEFICATE_CUCRBIT,
	SCOOTER,
	HOLE
};

#endif /* defined(__WittedMouse__MouseUtil__) */
